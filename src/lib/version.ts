/**
 * App version — used for cache invalidation and version display.
 * Increment this when deploying significant changes.
 */
export const APP_VERSION = '1.3.0';

/**
 * Version display name for UI
 */
export const VERSION_LABEL = 'v1.3';
